import styled from "styled-components";

const Body = styled.div`
  height: 64px;
  width: 120px;
  color: #ddd;
  position: absolute;
  right: -120px;
  top: 55px;
  font-size: 9px;

  p {
    margin: 0 0 10px 0;
    padding: 0;
  }
`;

export { Body };
